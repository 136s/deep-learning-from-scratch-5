name: dls5

channels:
  - conda-forge
  - pytorch  # for pytorch and torchvision

dependencies:
  - python>=3,<4
  - numpy>=1.24.2,<2
  - scipy>=1.10.1,<2
  - matplotlib>=3.7.1,<4
  - tqdm>=4.65.0,<5

  - pytorch>=2.0.0,<3
  - torchvision>=0.15.1

  - ipykernel>=6,<7
